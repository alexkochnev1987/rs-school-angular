import { ColoringButtonDirective } from './coloring-button.directive';

describe('ColoringButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoringButtonDirective();
    expect(directive).toBeTruthy();
  });
});
