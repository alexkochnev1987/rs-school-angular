import { ColoringBorderDirective } from './coloring-border.directive';

describe('ColoringBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoringBorderDirective();
    expect(directive).toBeTruthy();
  });
});
